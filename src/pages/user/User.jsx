import React, { useEffect, useState } from 'react';

function User({ }) {
    const [user, setUser] = useState([]);

    useEffect(() => {

    });

    return (
        <div></div>
    );
}

export default User;